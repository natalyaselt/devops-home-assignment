plugins {
    // Lets Gradle auto-download the required JDK (Java 21) if it isn't installed.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'now-time-service'
